import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'diepoe.heggame',
  appName: 'The HEGame',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
